apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ace-config-pvc
spec:
  storageClassName: ibmc-file-gold-gid
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem